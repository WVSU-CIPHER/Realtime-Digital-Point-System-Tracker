{
  "name": "fundays",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "php": "php -S localhost:8000",
    "vite-dev": "vite --open",
    "vite-build": "vite build --watch",
    "browser-sync": "browser-sync start --proxy 'http://localhost:8000' --files 'dist' --no-inject-changes",

    "dev": "run-p php vite-build browser-sync"
  },
  "devDependencies": {
    "vite": "^5.4.1"
  },
  "dependencies": {
    "glob": "^11.0.0"
  }
}
