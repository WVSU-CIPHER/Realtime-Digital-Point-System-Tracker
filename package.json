{
  "name": "fundays",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "vite-dev": "vite",
    "vite-watch": "vite build --watch",

    "vite-preview": "vite preview",
    
    "local-php": "php -S localhost:8000",
    "local-browser": "browser-sync start --proxy \"localhost:8000\" --files \"dist\"",
    "build": "vite build",

    "dev": "run-p vite-watch local-php local-browser"
  },
  "devDependencies": {
    "vite": "^5.4.1"
  },
  "dependencies": {
    "glob": "^11.0.0"
  }
}
